japan_korea_decision = {
	is_shown = {
		exists = c:JAP
		this = c:JAP
		NOT = {
			has_variable = jap_esports
		}
	}
	possible = {
		this = c:JAP
		owns_entire_state_region = STATE_YANGHO
		owns_entire_state_region = STATE_SARIWON
		owns_entire_state_region = STATE_PYONGYANG
		owns_entire_state_region = STATE_BUSAN
		owns_entire_state_region = STATE_SEOUL
	}
	when_taken = {
		set_variable = jap_esports
		s:STATE_SOUTHERN_MANCHURIA = {
			if = {
				limit = {
					any_scope_state = {
						NOT = {
							owner = ROOT
						}
					}
				}
				add_claim = ROOT
			}
		}
		s:STATE_NORTHERN_MANCHURIA = {
			if = {
				limit = {
					any_scope_state = {
						NOT = {
							owner = ROOT
						}
					}
				}
				add_claim = ROOT
			}
		}
		s:STATE_SHENGJING = {
			if = {
				limit = {
					any_scope_state = {
						NOT = {
							owner = ROOT
						}
					}
				}
				add_claim = ROOT
			}
		}
		s:STATE_FORMOSA = {
			if = {
				limit = {
					any_scope_state = {
						NOT = {
							owner = ROOT
						}
					}
				}
				add_claim = ROOT
			}
		}
	}
}

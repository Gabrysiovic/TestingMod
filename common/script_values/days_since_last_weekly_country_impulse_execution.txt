days_since_last_weekly_country_impulse_execution = {
    if = {
        limit = {
            NOT = {
                has_global_variable = date_of_last_event_execution
            }
        }
        value = 1
    }
    else = {
        value = game_date
        subtract = global_var:date_of_last_event_execution
        min = 0
    }
}